//! Template argument substitution — resolve `{{{1}}}`, `{{{name|default}}}`.
///
/// Phase 3 will implement full argument handling including defaults,
/// recursive expansion, and the `{{!}}` table pipe magic.

#[cfg(test)]
mod tests {
    // Tests will be added in Phase 3
}
